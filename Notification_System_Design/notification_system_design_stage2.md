Stage 2:
As data increases we need database
We can use mongodb fro results as the number of subjects may vary for different numbe rof students

Placements:
We can use MOngodb for this and result data

As the data changes and fields changes so we can use mongodb


Student:
{
    studentid,name,dob,semster,year
}
Results:
{
    studentid,resultid
    year,semester,
    subjectsmarks:[{array of objects}]
    
}